# logo_app

Запуск через VS Code:

- Откройте проект в VS Code.
- Откройте панель "Run" и выберите одну из конфигураций:
  - Run Debug
  - Run Profile
  - Run Release

Запуск через терминал:

```bash
flutter run
flutter run --profile
flutter run --release
```
